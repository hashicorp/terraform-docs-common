---
page_title: Reserved-tags API endpoint reference documentation
description: >-
    Use the `/reserved-tags` API endpoints to manage immutable tag keys for your organization so that project and workspace managers can follow a consistent tagging strategy.
---

# Reserved tags API reference

This topic provides reference information about the `/reserved-tags` API endpoints.

## Overview

Use the `/reserved-tags` API endpoints to manage immutable tag keys for your organization so that project and workspace managers can follow a consistent tagging strategy. HCP Terraform presents reserved tag keys as recommendations for projects within the organization. Refer to [Create a project](/terraform/cloud-docs/projects/manage#create-a-project) for additional information.

The following table describes the available endpoints:

| Method | Path | Description |
| --- | --- | --- |
| `GET` | `/reserved-tags/:reserved_tag_id` | [Shows details of the reserved tag key](#show-reserved-tag-key-details) with the specified ID. |
| `DELETE` | `/reserved-tags/:reserved_tag_id` | [Deletes a reserved tag key](#delete-a-reserved-tag-key) with the specified ID. |
| `GET` | `/organizations/:organization_name/reserved-tag-keys` | [Lists reserved tag keys](#lists-reserved-tag-keys) for the specified organization. |
| `POST` | `/organizations/:organization_name/reserved-tag-keys` | [Adds a reserved tag key](#add-a-reserved-tag-key) to the specified organization. |
| `PUT` | `/reserved-tags/:reserved_tag_id` |  [Updates the reserved tag key](#add-a-reserved-tag-value) for the specified ID. |


## Path parameters

The `/reserved-tags/ API endpoints take the following path parameters:

- `:reserved_tag_id`: External ID of the reserved tag object.
- `:organization_name`: Name of the organization associated with the reserved tags.

## Show reserved tag key details 

The following endpoint to returns a paginated list of reserved tag values for the specified ID:

`GET /reserved-tags/:reserved_tag_id`


### Sample payload

This endpoint does not require a payload.

### Sample request

```shell-session
curl \
  --header "Authorization: Bearer $TOKEN" \
  --header "Content-Type: application/vnd.api+json" \
  https://app.terraform.io/api/v2/reserved-tags/${RESERVED_TAG_ID}
```

### Sample response

```json

```


## Delete a reserved tag key

The following endpoint deletes a reserved tag key with the specified ID:

`DELETE /reserved-tags/:reserved_tag_id`



### Sample payload

This endpoint does not require a payload.

### Sample request

```shell-session
curl \
  --header "Authorization: Bearer $TOKEN" \
  --header "Content-Type: application/vnd.api+json" \
  --request DELETE \
  https://app.terraform.io/api/v2/reserved-tags/${RESERVED_TAG_ID}
```

### Sample response

```json

```

## Lists reserved tag keys

The following endpoint lists reserved tag keys for the specified organization:

`GET /organizations/:organization_name/reserved-tag-keys`


### Sample payload

This endpoint does not require a payload.

### Sample request

```shell-session
curl \
  --header "Authorization: Bearer $TOKEN" \
  --header "Content-Type: application/vnd.api+json" \
  --request GET \
  https://app.terraform.io/api/v2/organizations/${ORGANIZATION_NAME}/reserved-tag-keys
```

### Sample response

```json

```

## Add a reserved tag key 

The following endpoint adds a reserved tag key to the organization:

`POST /organizations/:organization_name/reserved-tag-keys` 


### Sample payload

TODO

### Sample request

```shell-session
curl \
  --header "Authorization: Bearer $TOKEN" \
  --header "Content-Type: application/vnd.api+json" \
  --request POST \
  https://app.terraform.io/api/v2/organizations/${ORGANIZATION_NAME}/reserved-tag-keys
```

### Sample response

```json

```

## Add a reserved tag value

The following endpoint adds a reserved tag value for the specified reserved key ID:

`POST /reserved-tags/:reserved_tag_id/value`

### Sample payload

This endpoint does not require a payload.

### Sample request

```shell-session
curl \
  --header "Authorization: Bearer $TOKEN" \
  --header "Content-Type: application/vnd.api+json" \
  --request POST \
  https://app.terraform.io/api/v2/reserved-tags/${RESERVED_TAG_ID}/new-value
```

### Sample response

```json

```